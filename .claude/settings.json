{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -Command \"$f = (Get-Content -Path $env:USERPROFILE -Raw -Encoding UTF8 | ConvertFrom-Json | Select-Object -ExpandProperty tool_input, tool_response | Where-Object { $_ -match '\\.py$' } | Select-Object -First 1); if ($f) { python -m py_compile $f 2>&1 | Out-Null }\"",
            "timeout": 30,
            "statusMessage": "Checking Python syntax..."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Read(D:/REPOGITHUB/AutomationMinecraftInternetBar/**)",
      "Write(D:/REPOGITHUB/AutomationMinecraftInternetBar/**)",
      "Edit(D:/REPOGITHUB/AutomationMinecraftInternetBar/**)",
      "Bash(python *)",
      "PowerShell(python *)"
    ]
  }
}
